# Simulated_Crypto_Exchange

Run the app:
- From repo root: `mvn -pl trading-service/trading-service spring-boot:run`
- Or from module dir: `cd trading-service/trading-service && mvn spring-boot:run`

Quick API test (PowerShell):