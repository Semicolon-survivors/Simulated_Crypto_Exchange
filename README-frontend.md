# Frontend (React) - User Dashboard

A modern React (Vite + TypeScript + MUI) dashboard that connects to the backend REST and WebSocket endpoints.

## Prerequisites
- Node.js >= 18
- Backend running locally on http://localhost:8080 (dev proxy is configured)

## Run in development
